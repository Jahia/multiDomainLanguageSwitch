// Custom commands for multi-domain-language-switch tests.
// cy.login(), cy.apollo(), cy.executeGroovy() and the other Jahia helpers come
// from @jahia/cypress registerSupport().
import 'cypress-wait-until';
